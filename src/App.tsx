import { useMemo, useState } from 'react';
import { ArrowLeft, ArrowRight, BarChart3, Bell, BookOpen, Bookmark, Brain, BriefcaseBusiness, Check, ChevronRight, CircleUser, Cross, Eye, FileClock, Flag, GraduationCap, HeartPulse, History, LayoutDashboard, LockKeyhole, Menu, Moon, Search, Settings, Shield, SlidersHorizontal, Sparkles, Sun, Target, Timer, Trophy, User, X } from 'lucide-react';

type Exam={id:string;title:string;full:string;category:string;inventory:string;icon:any;accent:string;description:string;tags:string[]};
type Page='home'|'exam'|'dashboard'|'practice-setup'|'question'|'analytics'|'study'|'history'|'bookmarks'|'settings'|'profile'|'pricing'|'signin';
const exams:Exam[]=[
{id:'pmp',title:'PMP',full:'Project Management Professional',category:'Project Management',inventory:'Demo bank · 2,000 items planned',icon:BriefcaseBusiness,accent:'violet',description:'Scenario-led project management preparation with focused practice, mock exams and decision-based review.',tags:['Scenario practice','Mock exams','Analytics']},
{id:'nclex',title:'NCLEX-RN',full:'National Council Licensure Examination',category:'Healthcare',inventory:'Demo bank · 3,000 items planned',icon:HeartPulse,accent:'cyan',description:'Clinical judgment practice with rationales and Next Generation case-study experiences.',tags:['NGN demo','Clinical judgment','Rationales']},
{id:'sergeant',title:'Police Sergeant',full:'Promotional Examination Prep',category:'Law Enforcement',inventory:'Demo bank · 500 items planned',icon:Shield,accent:'amber',description:'Promotional preparation centered on leadership, supervision, law and operational judgment.',tags:['USA Core demo','Leadership','Source UI']},
{id:'step1',title:'USMLE Step 1',full:'United States Medical Licensing Examination',category:'Medical',inventory:'Prototype · Coming later',icon:Cross,accent:'rose',description:'A future integrated foundational science and clinical reasoning experience.',tags:['Clinical reasoning','Foundations','Prototype']}
];
const demoQ={stem:'A project team is midway through an iteration when a stakeholder requests a high-priority change that may affect the current goal. What should the project manager do FIRST?',options:['Add the change immediately to avoid stakeholder dissatisfaction.','Assess the value and impact with the appropriate team members before deciding how to handle it.','Reject the request until the current release is complete.','Escalate the request directly to the project sponsor for approval.']};

export default function App(){
 const [page,setPage]=useState<Page>('home'),[exam,setExam]=useState(exams[0]),[query,setQuery]=useState(''),[menu,setMenu]=useState(false),[answer,setAnswer]=useState<number|null>(null),[submitted,setSubmitted]=useState(false),[theme,setTheme]=useState<'dark'|'light'|'system'>('dark');
 const filtered=useMemo(()=>exams.filter(e=>(e.title+e.full+e.category).toLowerCase().includes(query.toLowerCase())),[query]);
 const go=(p:Page)=>{setPage(p);setMenu(false);window.scrollTo({top:0,behavior:'smooth'})};
 const openExam=(e:Exam)=>{setExam(e);go('exam')};
 return <div className={`app ${theme==='light'?'light':''}`}>
  {page==='home'?<><SiteNav go={go} menu={menu} setMenu={setMenu}/><Home go={go} openExam={openExam} query={query} setQuery={setQuery} filtered={filtered}/></>:page==='signin'?<Auth go={go}/>:page==='exam'?<ExamPage exam={exam} go={go}/>:<AppShell page={page} go={go} exam={exam}><PageContent page={page} go={go} exam={exam} answer={answer} setAnswer={setAnswer} submitted={submitted} setSubmitted={setSubmitted} theme={theme} setTheme={setTheme}/></AppShell>}
 </div>
}
function SiteNav({go,menu,setMenu}:any){return <><nav><button className="brand" onClick={()=>go('home')}><span className="brandmark"><GraduationCap/></span><b>ASCEND</b><span>PREP</span></button><div className="navlinks"><button onClick={()=>document.getElementById('exams')?.scrollIntoView({behavior:'smooth'})}>Exam Library</button><button onClick={()=>go('dashboard')}>Product Demo</button><button onClick={()=>go('pricing')}>Pricing</button></div><div className="navactions"><button className="signin" onClick={()=>go('signin')}>Sign in</button><button className="primary small" onClick={()=>go('dashboard')}>Open demo <ArrowRight/></button></div><button className="menubtn" onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button></nav>{menu&&<div className="mobilemenu"><button onClick={()=>go('dashboard')}>Product Demo</button><button onClick={()=>go('pricing')}>Pricing</button><button onClick={()=>go('signin')}>Sign in</button></div>}</>}
function Home({go,openExam,query,setQuery,filtered}:any){return <main><section className="hero newHero"><div className="heroCopy"><div className="eyebrow"><Sparkles/> VISUAL PROTOTYPE · DEMO DATA</div><h1>One place to prepare for <span>what comes next.</span></h1><p>A premium exam-prep experience designed around deliberate practice, clear explanations, realistic simulations and actionable progress.</p><div className="heroActions"><button className="primary" onClick={()=>go('dashboard')}>Explore product demo <ArrowRight/></button><button className="ghost" onClick={()=>document.getElementById('exams')?.scrollIntoView({behavior:'smooth'})}>Choose an exam</button></div><div className="trust"><span><Check/>Clickable prototype</span><span><Check/>Responsive experience</span><span><Check/>No real user data</span></div></div><ProductPreview go={go}/></section><section className="examSection" id="exams"><div className="sectionHead"><div><span className="kicker">CHOOSE YOUR PATH</span><h2>What are you preparing for?</h2><p>Each exam gets its own focused experience while sharing one learning platform.</p></div><label className="search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search exams or fields..."/></label></div><div className="examGrid">{filtered.map((e:Exam)=><ExamCard key={e.id} exam={e} onClick={()=>openExam(e)}/>)}</div></section><section className="features"><div className="featureIntro"><span className="kicker">PRODUCT EXPERIENCE</span><h2>Designed around the study loop.</h2><p>Choose a goal, practice deliberately, understand the rationale, then use performance signals to decide what to study next.</p></div><div className="featureGrid"><Feature icon={SlidersHorizontal} n="01" title="Build a session" text="Choose topics, difficulty, mode and question count."/><Feature icon={Brain} n="02" title="Understand decisions" text="Review the reasoning behind correct and incorrect options."/><Feature icon={BarChart3} n="03" title="Read your signals" text="See demo strengths, weak areas and response-time trends."/><Feature icon={Target} n="04" title="Plan the next move" text="Turn performance into a focused next-session recommendation."/></div></section><footer><div className="brand"><span className="brandmark"><GraduationCap/></span><b>ASCEND</b><span>PREP</span></div><p>Visual prototype for professional exam preparation.</p><span>© 2026 Demo</span></footer></main>}
function ProductPreview({go}:any){return <div className="productWindow"><div className="windowTop"><span><i/><i/><i/></span><b>Practice · PMP Demo</b><small>12 / 20</small></div><div className="previewBody"><div className="previewRail"><LayoutDashboard/><BookOpen className="active"/><BarChart3/><Settings/></div><div className="previewMain"><div className="previewMeta"><span>PROCESS · DEMO QUESTION</span><Timer/> 01:18</div><h3>A stakeholder requests a significant change during an active iteration. What should the project manager do first?</h3><div className="previewOptions"><span>A&nbsp;&nbsp; Implement it immediately</span><span className="selected">B&nbsp;&nbsp; Assess value and impact with the team <Check/></span><span>C&nbsp;&nbsp; Reject it until release</span><span>D&nbsp;&nbsp; Escalate directly to sponsor</span></div><div className="explainStrip"><Brain/><div><small>WHY THIS WORKS</small><b>Evaluate the request before changing committed work.</b></div><button onClick={()=>go('question')}>Open practice <ChevronRight/></button></div></div></div></div>}
function ExamCard({exam,onClick}:{exam:Exam,onClick:()=>void}){const Icon=exam.icon;return <button className={'examCard '+exam.accent} onClick={onClick}><div className="cardTop"><span className="examIcon"><Icon/></span><span className="count">PROTOTYPE</span></div><small>{exam.category.toUpperCase()}</small><h3>{exam.title}</h3><h4>{exam.full}</h4><p>{exam.description}</p><div className="inventory">{exam.inventory}</div><div className="tags">{exam.tags.map(t=><span key={t}>{t}</span>)}</div><div className="cardCta">View experience <ArrowRight/></div></button>}
function Feature({icon:Icon,n,title,text}:any){return <div className="featureCard"><span className="num">{n}</span><span className="featureIcon"><Icon/></span><h3>{title}</h3><p>{text}</p></div>}
function ExamPage({exam,go}:{exam:Exam,go:any}){const Icon=exam.icon;return <div className="examLanding"><button className="backLink" onClick={()=>go('home')}><ArrowLeft/> All exams</button><div className="examHero"><div><span className={'examIcon xl '+exam.accent}><Icon/></span><div className="eyebrow">{exam.category} · Visual Demo</div><h1>{exam.title}</h1><h3>{exam.full}</h3><p>{exam.description}</p><div className="heroActions"><button className="primary" onClick={()=>go('practice-setup')}>Build practice session <ArrowRight/></button><button className="ghost" onClick={()=>go('dashboard')}>View dashboard</button></div></div><div className="examPanel"><small>DEMO EXPERIENCE</small><h3>Your study workspace</h3>{['Focused practice builder','Timed mock exam interface','Detailed answer review','Performance & readiness UI'].map(x=><span key={x}><Check/>{x}</span>)}<div className="notice">Prototype inventory and metrics are illustrative only.</div></div></div><div className="journey"><button onClick={()=>go('practice-setup')}><SlidersHorizontal/><b>Practice</b><span>Configure a focused session</span></button><button onClick={()=>go('dashboard')}><LayoutDashboard/><b>Dashboard</b><span>See your daily study hub</span></button><button onClick={()=>go('analytics')}><BarChart3/><b>Performance</b><span>Explore demo analytics</span></button><button onClick={()=>go('study')}><Target/><b>Study Plan</b><span>Plan the next milestone</span></button></div></div>}
function AppShell({page,go,exam,children}:any){const items=[['dashboard',LayoutDashboard,'Dashboard'],['practice-setup',BookOpen,'Practice'],['analytics',BarChart3,'Performance'],['study',Target,'Study Plan'],['history',History,'History'],['bookmarks',Bookmark,'Bookmarks'],['settings',Settings,'Settings']];return <div className="shell"><aside><button className="brand" onClick={()=>go('home')}><span className="brandmark"><GraduationCap/></span><b>ASCEND</b></button><div className="examSwitch"><small>CURRENT EXAM</small><b>{exam.title}</b><span>Prototype workspace</span></div><div className="sideNav">{items.map(([p,I,l]:any)=><button className={page===p?'active':''} onClick={()=>go(p)} key={p}><I/>{l}</button>)}</div><div className="sideBottom"><button onClick={()=>go('profile')}><CircleUser/>Demo Student</button><span>Visual prototype</span></div></aside><div className="shellMain"><header><div><small>{exam.title} · DEMO</small><b>{page==='practice-setup'?'Practice':page.replace('-',' ')}</b></div><div><button><Bell/></button><button onClick={()=>go('profile')}><User/></button></div></header>{children}</div></div>}
function PageContent({page,go,exam,answer,setAnswer,submitted,setSubmitted,theme,setTheme}:any){if(page==='dashboard')return <Dashboard go={go} exam={exam}/>;if(page==='practice-setup')return <PracticeSetup go={go}/>;if(page==='question')return <Question go={go} answer={answer} setAnswer={setAnswer} submitted={submitted} setSubmitted={setSubmitted}/>;if(page==='analytics')return <Analytics/>;if(page==='study')return <Study/>;if(page==='history')return <SimplePage icon={History} title="Question History" text="Review recent demo practice activity."/>;if(page==='bookmarks')return <SimplePage icon={Bookmark} title="Bookmarks" text="Saved demo questions appear here for later review."/>;if(page==='settings')return <SettingsPage theme={theme} setTheme={setTheme}/>;if(page==='profile')return <Profile/>;if(page==='pricing')return <Pricing go={go}/>;return null}
function Dashboard({go,exam}:any){return <div className="page"><div className="pageTitle"><div><span className="kicker">FRIDAY · DEMO WORKSPACE</span><h1>Good evening, Demo Student.</h1><p>Continue your {exam.title} prototype journey.</p></div><span className="demoBadge">DEMO DATA</span></div><button className="continueCard" onClick={()=>go('question')}><div><small>CONTINUE WHERE YOU LEFT OFF</small><h2>Adaptive Practice · Process</h2><p>Question 12 of 20 · Tutor mode</p></div><span className="play"><ArrowRight/></span></button><div className="metricGrid"><Metric label="Readiness estimate" value="74%" note="Prototype estimate"/><Metric label="Demo accuracy" value="78%" note="+4 pts this week"/><Metric label="Questions reviewed" value="428" note="Illustrative data"/><Metric label="Study streak" value="9 days" note="Demo activity"/></div><div className="dashGrid"><div className="panel"><div className="panelHead"><div><small>DOMAIN PERFORMANCE</small><h3>Where to focus next</h3></div><button onClick={()=>go('analytics')}>Full report <ChevronRight/></button></div><Bars/></div><div className="panel recommend"><Brain/><small>RECOMMENDED NEXT</small><h3>Strengthen Process fundamentals</h3><p>Your demo performance suggests more practice in risk and change scenarios.</p><button className="primary" onClick={()=>go('practice-setup')}>Build 25-question session</button></div></div></div>}
function Metric({label,value,note}:any){return <div className="metric"><span>{label}</span><b>{value}</b><small>{note}</small></div>}
function Bars(){return <div className="bars">{[['People',84],['Process',68],['Business Environment',76],['Mixed Scenarios',73]].map(([n,v]:any)=><div key={n}><span>{n}<b>{v}%</b></span><i><em style={{width:v+'%'}}/></i></div>)}</div>}
function PracticeSetup({go}:any){const [count,setCount]=useState(25),[mode,setMode]=useState('Tutor');return <div className="page narrow"><div className="pageTitle"><div><span className="kicker">PRACTICE BUILDER</span><h1>Build your next session.</h1><p>All controls are local demo interactions.</p></div></div><div className="setupCard"><h3>Session mode</h3><div className="segmented">{['Tutor','Timed'].map(x=><button className={mode===x?'active':''} onClick={()=>setMode(x)}>{x}</button>)}</div><h3>Question count</h3><div className="countChoices">{[10,25,50,100].map(x=><button className={count===x?'active':''} onClick={()=>setCount(x)}>{x}</button>)}</div><h3>Topics</h3><div className="checkGrid">{['People','Process','Business Environment','Mixed Scenarios'].map((x,i)=><label><input type="checkbox" defaultChecked={i<2}/><span><Check/></span>{x}</label>)}</div><h3>Difficulty</h3><div className="segmented"><button>Foundation</button><button className="active">Mixed</button><button>Advanced</button></div><div className="setupSummary"><div><small>SESSION PREVIEW</small><b>{count} questions · {mode} mode</b></div><button className="primary" onClick={()=>go('question')}>Start demo session <ArrowRight/></button></div></div></div>}
function Question({go,answer,setAnswer,submitted,setSubmitted}:any){return <div className="questionPage"><div className="qTop"><button onClick={()=>go('practice-setup')}><X/></button><div><b>PMP Demo Practice</b><span>12 of 20</span></div><div><button><Bookmark/></button><button><Flag/></button><span><Timer/> 01:24</span></div></div><div className="qProgress"><i/></div><div className="qWrap"><main><div className="qMeta"><span>PROCESS</span><span>MODERATE</span><span>DEMO QUESTION</span></div><h1>{demoQ.stem}</h1><div className="answers">{demoQ.options.map((o,i)=><button disabled={submitted} className={`${answer===i?'selected ':''}${submitted&&i===1?'correct ':''}${submitted&&answer===i&&i!==1?'wrong ':''}`} onClick={()=>setAnswer(i)}><b>{String.fromCharCode(65+i)}</b><span>{o}</span>{submitted&&i===1&&<Check/>}</button>)}</div>{!submitted?<button className="primary submit" disabled={answer===null} onClick={()=>setSubmitted(true)}>Submit answer</button>:<Explanation go={go}/>}</main><aside className="qSide"><small>SESSION</small><b>Tutor Mode</b><div><span>Answered</span><b>11</b></div><div><span>Remaining</span><b>9</b></div><div><span>Bookmarked</span><b>2</b></div><p>Prototype data only.</p></aside></div></div>}
function Explanation({go}:any){return <div className="explanation"><div className="result"><span><Check/></span><div><small>CORRECT ANSWER · DEMO</small><h3>Assess the request before changing committed work.</h3></div></div><p>In this prototype rationale, the best first step is to understand value, impact and context with the appropriate team members before deciding how the requested change should be handled.</p><h4>Why the other options are weaker</h4><div className="rationales"><span><b>A</b>Immediate implementation bypasses impact assessment.</span><span><b>C</b>Automatic rejection ignores potential business value.</span><span><b>D</b>Escalation is premature before the team understands the request.</span></div><div className="sourceBox"><BookOpen/><div><small>REFERENCE PLACEHOLDER</small><b>Official/source citation would appear here after content verification.</b></div></div><button className="primary" onClick={()=>{go('question')}}>Next demo question <ArrowRight/></button></div>}
function Analytics(){return <div className="page"><div className="pageTitle"><div><span className="kicker">PERFORMANCE</span><h1>Turn practice into direction.</h1><p>Illustrative analytics for visual evaluation only.</p></div><span className="demoBadge">DEMO DATA</span></div><div className="metricGrid"><Metric label="Accuracy" value="78%" note="Demo trend +4"/><Metric label="Avg. response" value="01:18" note="Demo timing"/><Metric label="Coverage" value="64%" note="Prototype coverage"/><Metric label="Readiness" value="74%" note="Estimated demo score"/></div><div className="dashGrid"><div className="panel"><small>DOMAIN BREAKDOWN</small><h3>Performance by domain</h3><Bars/></div><div className="panel"><small>RECENT SESSIONS</small><h3>Practice history</h3><div className="sessionList">{[['Process · 25 Q','80%'],['People · 20 Q','85%'],['Mixed · 50 Q','72%'],['Business · 15 Q','76%']].map(x=><div><span>{x[0]}</span><b>{x[1]}</b></div>)}</div></div></div></div>}
function Study(){return <div className="page"><div className="pageTitle"><div><span className="kicker">STUDY PLAN</span><h1>A clear next step, every day.</h1><p>Demo plan generated from illustrative performance.</p></div></div><div className="timeline"><Plan when="TODAY" title="25-question Process session" text="Risk, change and stakeholder scenarios" done={false}/><Plan when="THIS WEEK" title="Complete Mock Exam 02" text="Review every incorrect answer afterward" done={false}/><Plan when="NEXT MILESTONE" title="Reach 75% topic coverage" text="Current demo coverage: 64%" done={false}/></div></div>}
function Plan({when,title,text}:any){return <div className="plan"><span><Target/></span><div><small>{when}</small><h3>{title}</h3><p>{text}</p></div><button><ChevronRight/></button></div>}
function SimplePage({icon:Icon,title,text}:any){return <div className="page"><div className="pageTitle"><div><span className="kicker">LIBRARY</span><h1>{title}</h1><p>{text}</p></div></div><div className="empty"><span><Icon/></span><h2>{title} demo</h2><p>This prototype area demonstrates the intended empty-state design. Real account data will be connected later.</p></div></div>}
function SettingsPage({theme,setTheme}:any){const [notify,setNotify]=useState(true),[timer,setTimer]=useState(true);return <div className="page"><div className="pageTitle"><div><span className="kicker">PREFERENCES</span><h1>Settings</h1><p>Clickable local-state controls for prototype evaluation.</p></div></div><div className="settingsLayout"><div className="settingsNav">{['Appearance','Study Preferences','Notifications','Accessibility','Account','Privacy'].map((x,i)=><button className={i===0?'active':''}>{x}</button>)}</div><div className="settingsContent"><section><h3>Appearance</h3><p>Choose how the prototype looks on this device.</p><div className="themeCards">{[['light',Sun,'Light'],['dark',Moon,'Dark'],['system,',Eye,'System']].map(([v,I,l]:any)=><button className={theme===v?'active':''} onClick={()=>setTheme(v==='system,'?'system':v)}><I/><b>{l}</b><small>{l==='System'?'Follow device':'Use '+l.toLowerCase()+' appearance'}</small></button>)}</div></section><section><h3>Study Preferences</h3><div className="settingRow"><div><b>Show timer by default</b><span>Display elapsed time during practice.</span></div><Toggle on={timer} set={setTimer}/></div><div className="settingRow"><div><b>Default session size</b><span>Used when starting quick practice.</span></div><select><option>25 questions</option><option>10 questions</option><option>50 questions</option></select></div></section><section><h3>Notifications</h3><div className="settingRow"><div><b>Study reminders</b><span>Demo notification preference.</span></div><Toggle on={notify} set={setNotify}/></div></section><section><h3>Accessibility</h3><div className="settingRow"><div><b>Text size</b><span>Prototype reading preference.</span></div><select><option>Default</option><option>Large</option><option>Extra large</option></select></div></section></div></div></div>}
function Toggle({on,set}:any){return <button className={'toggle '+(on?'on':'')} onClick={()=>set(!on)}><i/></button>}
function Profile(){return <div className="page narrow"><div className="pageTitle"><div><span className="kicker">DEMO ACCOUNT</span><h1>Profile</h1><p>Visual placeholder — no real account is connected.</p></div></div><div className="profileCard"><span className="avatar">DS</span><div><h2>Demo Student</h2><p>demo@example.com</p><span className="demoBadge">PROTOTYPE ACCOUNT</span></div></div><div className="setupCard"><h3>Learning goal</h3><p>PMP · Prototype workspace</p><h3>Target date</h3><p>Not connected</p><button className="outline">Edit demo profile</button></div></div>}
function Pricing({go}:any){return <div className="standalone"><button className="backLink" onClick={()=>go('home')}><ArrowLeft/> Home</button><div className="centerHead"><span className="kicker">PRICING PROTOTYPE</span><h1>Choose the access that fits your goal.</h1><p>Prices are intentionally not set during the visual prototype phase.</p></div><div className="priceCards big"><Price title="Focused Access" tag="SINGLE EXAM" items={['One exam workspace','Practice & mock exam UI','Performance analytics','Study plan']}/><Price title="Ascend Complete" tag="ALL ACCESS · DEMO" items={['Every exam workspace','Unified dashboard','All analytics views','Future categories']}/></div></div>}
function Price({title,tag,items}:any){return <div className="price"><small>{tag}</small><h2>{title}</h2><p>Pricing to be determined after prototype validation.</p><ul>{items.map((x:string)=><li><Check/>{x}</li>)}</ul><button className="primary">Preview checkout <ArrowRight/></button></div>}
function Auth({go}:any){const [create,setCreate]=useState(false);return <div className="auth"><button className="brand authBrand" onClick={()=>go('home')}><span className="brandmark"><GraduationCap/></span><b>ASCEND PREP</b></button><div className="authCard"><span className="demoBadge">DEMO ONLY</span><h1>{create?'Create your account':'Welcome back'}</h1><p>No authentication is connected in this prototype.</p><label>Email<input placeholder="you@example.com"/></label><label>Password<div className="password"><input type="password" placeholder="••••••••"/><Eye/></div></label><button className="primary wide" onClick={()=>go('dashboard')}>{create?'Create demo account':'Enter demo workspace'}<ArrowRight/></button><button className="textBtn" onClick={()=>setCreate(!create)}>{create?'Already have an account? Sign in':'New here? Create account'}</button><button className="textBtn">Forgot password?</button></div></div>}
